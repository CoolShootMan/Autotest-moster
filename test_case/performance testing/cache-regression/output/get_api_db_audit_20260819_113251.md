# GET API DB 读取审计报告

- 生成时间：2026-08-19 11:32:51
- GET 请求总数：13
- 去重 GET 接口数：1

## 结果总览

- **违规接口数（预热后仍读 DB）：1**

## 违规接口（预热后仍读 DB，需关注）

| # | GET URL | 请求顺序→DB数 |
|---|---------|--------------|
| 1 | `https://release.katana-api.1m.app/posts/consumer/detail?vanityUrl=resident&urlAlias=11756` | #1(DB=11, HTTP=200) → #2(DB=0, HTTP=200) → #3(DB=3, HTTP=200) → #4(DB=0, HTTP=200) → #5(DB=0, HTTP=200) → #6(DB=0, HTTP=200) → #7(DB=0, HTTP=200) → #8(DB=0, HTTP=200) → #9(DB=0, HTTP=200) → #10(DB=0, HTTP=200) → #11(DB=0, HTTP=200) → #12(DB=0, HTTP=200) → #13(DB=0, HTTP=200) |

## 全部 GET 明细

| # | GET URL | 请求次数 | DB 分布 | 状态 |
|---|---------|---------|---------|------|
| 1 | `https://release.katana-api.1m.app/posts/consumer/detail?vanityUrl=resident&urlAlias=11756` | 13 | 0DB×11次 | 3DB×1次 | 11DB×1次 | 违规(预热后未命中) |

## 判定口径

- 首次请求（冷启动/预热）DB>0 视为正常穿透，仅记录不判定违规；
- 同一 GET URL 第 2 次及以后仍 DB>0 → 缓存未命中，判定违规并抛出；
- 非 2xx 响应的 DB 计数不可信，不参与违规判定；
- header_integrity_check 的 /feature-flag/ 探测请求已过滤；
- DB=-1 表示 X-DB-Query-Count header 缺失（后端埋点未部署）。
